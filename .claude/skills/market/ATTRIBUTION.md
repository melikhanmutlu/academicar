# AI Marketing Suite — Attribution

These `market*` skills and `market-*` agents are adapted from the open-source
**AI Marketing Suite for Claude Code** by zubair-trabzada (MIT License).

- Source: https://github.com/zubair-trabzada/ai-marketing-claude
- License: MIT (see `LICENSE` in this folder)

Adaptation for AcademicAR: each `SKILL.md` and agent file was given the YAML
frontmatter (`name` / `description`) that Claude Code requires for discovery,
and the files were installed under this repo's `.claude/skills/` and
`.claude/agents/` so they are version-controlled with the project rather than a
machine-global `~/.claude` install.
